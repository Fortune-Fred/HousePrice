# 北北桃房價地圖 v1 — 執行規格書

> 本文件自成一體：執行者不需要任何先前對話的上下文。
> 所有決策已由使用者拍板，不要重新開放討論。

---

## 0. 執行守則（先讀這段）

1. **依步驟順序執行**（步驟 1 → 13），每完成一步就更新文末「進度勾選區」。
2. **每步都有驗收條件**，未通過驗收不得標記完成、不得進入下一步。
3. **遇到本規格沒有涵蓋的決策，停下來問使用者**，不要自由發揮。
4. **不得偽造或內插不存在的資料**。沒有成交資料的地方就是灰色，寧可空白也不要假地形。
5. 資料處理過程中丟棄了多少筆、為什麼丟，都要留下紀錄（log 或 report 檔）。
6. 中途 context 被清除時，重讀本檔案與進度勾選區即可接續。

---

## 1. 專案目的與背景

使用者要自住買房（觀察期約一兩年），想要一張互動地圖：
用房價與交易熱度直覺看出台北、新北、桃園的「蛋黃區／蛋白區」分布，
輔助「哪些區域不要買」的排除式決策。

**v1 明確只做價格快照地圖**（哪裡貴、哪裡交易熱）。
**保值分析（漲幅、抗跌）是 v2**，v1 不做、也不宣稱能回答保值。

---

## 2. 已拍板的產品決策（不要重新討論）

| 項目 | 決策 |
|---|---|
| 地理範圍 | 台北市、新北市、桃園市 |
| 空間粒度 | **里**（村里界），不用行政區、不用網格 |
| 資料窗口 | 近 8 季（2 年）滾動；里成交數不足時擴到 12 季 |
| 視圖（三選一切換） | ① 每坪單價（中位數） ② 總價（中位數） ③ 成交量 |
| 統計量 | 一律用**中位數**，不用平均（抗異常值） |
| 篩選器 | 總價滑桿（範圍外的里灰掉）＋ 預售/中古切換 |
| 物件範圍 | 自住住宅全留：公寓、住宅大樓、華廈、透天、套房；預售屋留（但可切換分開看）；無電梯老公寓留 |
| 排除 | 純車位交易、店面、辦公、廠辦、工廠、倉庫、純土地 |
| 無資料區處理 | 山區、水域、成交不足的里 → 灰色，**禁止內插** |
| 更新能力 | 一鍵重抓最新一季並重新產出（步驟 12） |
| 呈現 | 互動網頁地圖，顏色深淺表現數值，可選 3D 高度起伏 |

**v2 待辦（v1 不做）**：5~10 年年化漲幅、2022–23 修正期抗跌度、低流動性風險標記。

---

## 3. 資料來源

1. **實價登錄開放資料（內政部地政司）**
   - 批次下載頁：https://plvr.land.moi.gov.tw/DownloadOpenData
   - 歷史季度 zip 格式：`https://plvr.land.moi.gov.tw/DownloadSeason?season={民國年}S{季}&type=zip&fileName=lvr_landcsv.zip`（例：`113S3`）
   - 縣市代碼：A=台北市、F=新北市、H=桃園市
   - 檔名尾碼：`_a` = 中古買賣、`_b` = 預售屋（`_c` 租賃不要）
   - 實際 URL/欄位若與上述不符，以現場探查為準，探查結果記錄下來。
2. **村里界圖資**：政府資料開放平臺（data.gov.tw）搜尋「村里界圖 TWD97 經緯度」（內政部國土測繪中心）。
3. **地址轉座標（geocoding）**：候選方案見步驟 7，需實測後決定。
   注意：實價登錄門牌是去識別化區間（如「文林路1~30號」），取區間中點定位即可，誤差可接受。

---

## 4. 資料清洗規則

1. 只保留「交易標的」含建物之住宅交易（房地、房地+車位）；排除純土地、純車位。
2. 「建物型態」保留：公寓、住宅大樓、華廈、透天厝、套房；其餘（店面、辦公、廠辦、工廠、倉庫等）排除。
3. **車位拆算**：交易含車位且「車位總價」欄有值時，總價減車位價、建物面積減車位面積後再算單價；車位資訊不全無法拆算的，該筆標記 flag，仍保留但在報告中統計數量。
4. 剔除「備註」欄含「親友」「特殊關係」「債務」「急買急賣」等關鍵字的交易。
5. 單價 sanity 界線：每坪 < 5 萬或 > 350 萬的剔除並記錄；另對每個行政區剔除單價 P1 以下、P99 以上的極端值。
6. 單價一律重算為**每坪**：`(總價 − 車位價) ÷ ((建物移轉面積 − 車位面積) × 0.3025)`（平方公尺→坪）。
7. 每一條規則丟棄的筆數都要輸出到 `reports/cleaning_report.md`。

---

## 5. 聚合規則

- 分組：里 × 物件類別（全部 / 中古 / 預售）三套指標。
- 指標：單價中位數、總價中位數、成交筆數。
- 窗口：預設近 8 季；某里某類別筆數 < 10 → 擴到近 12 季；仍 < 10 → 該里該類別標記 `insufficient`（前端顯示灰色）。
- 輸出：一份 GeoJSON（里界 polygon + 上述屬性），供前端直接使用。

---

## 6. 技術選型

- 資料管線：Python 3.11+（pandas / geopandas / shapely），依套件管理用 `uv` 或 `pip + requirements.txt`。
- 前端：**MapLibre GL JS**（免費、支援 fill-extrusion 3D）＋ 靜態 GeoJSON，純靜態網站（無後端），本地以簡單 http server 預覽。
- 底圖：免費瓦片源（如 OpenStreetMap raster 或 MapLibre demotiles），不得使用需付費金鑰的服務。
- 專案結構建議：
  ```
  C:\grillme\
    SPEC.md
    pipeline\        # 下載、清洗、geocode、聚合腳本
    data\raw\        # 原始 zip/csv（gitignore）
    data\processed\  # parquet、GeoJSON
    reports\         # 清洗報告、geocode 成功率報告
    web\             # 前端靜態網站
    update.py        # 一鍵更新入口
  ```

---

## 7. 執行步驟

### 步驟 1：環境與專案骨架
建立上述目錄結構、Python 虛擬環境、安裝依賴、git init（若尚未）。
**驗收**：`python -c "import pandas, geopandas"` 成功；目錄結構存在。

### 步驟 2：下載實價登錄歷史批次
寫 `pipeline/download.py`：下載近 12 季、A/F/H 三縣市、`_a` 與 `_b` 檔，存 `data/raw/`，已存在的季度跳過（可重跑）。
**驗收**：`data/raw/` 下有 12 季 × 3 縣市的 CSV；隨機開 2 個檔案確認欄位含「交易標的」「總價元」「土地位置建物門牌」。

### 步驟 3：解析合併
寫 `pipeline/parse.py`：合併所有 CSV（注意實價登錄 CSV 第二列是英文欄名列，需跳過），統一欄位、標記縣市／季度／中古或預售，輸出 `data/processed/raw_merged.parquet`。
**驗收**：印出總筆數與各縣市筆數，三縣市皆 > 0；隨機抽 5 筆與原始 CSV 對照一致。

### 步驟 4：清洗過濾
寫 `pipeline/clean.py`：實作第 4 節規則 1–5，輸出 `data/processed/cleaned.parquet` 與 `reports/cleaning_report.md`。
**驗收**：報告列出每條規則的丟棄筆數；剩餘資料的建物型態只含允許清單。

### 步驟 5：單價重算
實作第 4 節規則 3、6。
**驗收**：抽 10 筆含車位交易，手算驗證拆算正確；全體單價分布印出 P5/P50/P95，落在常識範圍（北市 P50 約 50–90 萬/坪量級，若嚴重偏離即為 bug）。

### 步驟 6：村里界圖資
下載村里界圖，過濾出北北桃，必要時簡化幾何（保留拓撲），輸出 `data/processed/villages.geojson`。
**驗收**：北北桃里數量合理（約 2,600–3,000 個里）；隨機抽 3 個知名里（如台北市大安區龍安里）確認 polygon 位置正確。

### 步驟 7：Geocoding 方案評估（可能需要使用者配合）
候選：TGOS 全國門牌地址定位服務（需申請帳號）、國土測繪中心 API、其他免費方案。
先抽樣 1,000 筆地址實測成功率與速度，寫入 `reports/geocode_eval.md`。
**若最佳方案需要使用者申請帳號/金鑰，停下來請使用者辦理。**
**驗收**：選定方案成功率 ≥ 80%；報告記錄各方案比較。成功率不足時停下來與使用者討論退路（如降級到網格）。

### 步驟 8：全量 geocode 與歸里
寫 `pipeline/geocode.py`：全量地址轉座標，**建立地址→座標快取**（之後更新只需處理新地址）；point-in-polygon 歸到里。
**驗收**：歸里成功率 ≥ 80% 並記錄於報告；抽 10 筆人工驗證地址與里對得上。

### 步驟 9：聚合計算
寫 `pipeline/aggregate.py`：實作第 5 節，輸出 `data/processed/map_data.geojson`。
**驗收**：抽查大安區任一里單價中位數 > 桃園市郊區任一里；`insufficient` 的里數量印出且比例 < 40%（若更高，停下來回報）。

### 步驟 10：前端地圖
`web/` 下建 MapLibre 靜態頁：載入 `map_data.geojson`，choropleth 上色 + 可切換 3D extrusion，三視圖切換（單價／總價／成交量），滑鼠 hover 顯示里名與數值，灰色顯示 `insufficient` 與無資料區。
**驗收**：本地 server 開啟後，肉眼確認：台北市中心顏色最深、山區（陽明山、烏來）灰色、三個視圖切換正常。

### 步驟 11：篩選器
加入：總價滑桿（里的總價中位數超出範圍 → 灰掉）、預售/中古/全部三段切換（切換時讀取對應那套指標）。
**驗收**：滑桿拉到 1,500 萬上限時台北市蛋黃區大面積灰掉；切到「預售」時桃園青埔一帶數值明顯高於「中古」。

### 步驟 12：一鍵更新
寫 `update.py`：偵測並下載最新一季 → 重跑清洗/geocode（用快取，只處理新增）→ 重新聚合 → 重產 GeoJSON。
**驗收**：完整跑一次 `python update.py` 成功結束且冪等（連跑兩次結果一致）。

### 步驟 13：驗收走查
對照常識抽查 10 個地點：大安區（高單價）、信義區（高總價）、萬華老城區（單價相對低）、板橋江翠（中高）、林口/青埔（預售高、中古中等）、烏來/陽明山（灰色）、桃園偏鄉里（灰色或低）。任何一項與常識矛盾都要查明原因（是 bug 還是市場事實），寫入 `reports/final_qa.md`。
**驗收**：`final_qa.md` 完成，矛盾項皆有解釋。

---

## 8. 進度勾選區（執行者維護）

- [x] 步驟 1：環境與專案骨架
- [x] 步驟 2：下載實價登錄歷史批次
- [x] 步驟 3：解析合併
- [x] 步驟 4：清洗過濾
- [x] 步驟 5：單價重算
- [x] 步驟 6：村里界圖資
- [x] 步驟 7：Geocoding 方案評估
- [ ] 步驟 8：全量 geocode 與歸里
- [ ] 步驟 9：聚合計算
- [ ] 步驟 10：前端地圖
- [ ] 步驟 11：篩選器
- [ ] 步驟 12：一鍵更新
- [ ] 步驟 13：驗收走查

## 9. 執行紀錄（執行者維護：每步完成後一行摘要，含日期與重要發現）

- 2026-07-18 步驟 1：目錄結構＋.venv（Python 3.14.4）＋pandas 3.0.3 / geopandas 1.1.4 安裝成功，git init 完成。系統無 uv，改用 pip + requirements.txt。
- 2026-07-18 步驟 2：12 季（112S3–115S2）× 3 縣市 × _a/_b 下載完成（115S3 尚未釋出）。發現：plvr 網站憑證鏈缺 SKI，需用 truststore 走系統憑證庫；CSV 為 UTF-8，第二列是英文欄名列。欄位驗證通過。
- 2026-07-18 步驟 3：合併 497,839 筆（北市 88,409／新北 212,773／桃園 196,657；中古 386,108／預售 111,731）。發現：5 列欄位數異常（未引號逗號），已併回最後一欄。抽 5 筆對照原始 CSV 全數一致。
- 2026-07-18 步驟 4：清洗後 389,512 筆（保留率 78.2%），報告見 reports/cleaning_report.md。⚠ 規格外決策：剔除 583 筆預售「解約」交易（非真實成交），請使用者複核。48,295 筆含車位但無法拆算，flag 保留。
- 2026-07-18 步驟 5：車位拆算抽 10 筆手算全數一致。單價 P5/P50/P95（萬/坪）：北市 48.1/85.6/150.5、新北 24.8/48.9/79.0、桃園 17.9/32.1/51.6，符合常識。
- 2026-07-18 步驟 6：村里界（data.gov.tw dataset 7438，1150624 版）過濾北北桃得 2,041 里（北市 456／新北 1,056／桃園 529），行政區數 12/29/13 正確，龍安里/江翠里/青埔里 polygon 位置驗證通過。⚠ 規格預估 2,600–3,000 偏高，實際官方里數即約 2,041，視為驗收通過。幾何已簡化（容差 1e-4 度），輸出 3.3 MB。
- 2026-07-18 步驟 7：NLSC TextQueryMap（免帳號，需帶 Referer）抽樣 1,000 筆成功率 **97.0%**，約 1.06 秒/筆，報告見 reports/geocode_eval.md。不需申請 TGOS 帳號（列為備援）。加分發現：API 回應直接含里名，步驟 8 可雙軌驗證。待決：全量唯一地址約 13 萬筆，單執行緒需 ~36 小時，併發數/跑法待使用者拍板。→ 使用者拍板：併發 4。
- 2026-07-18 步驟 8：pipeline/geocode.py 完成（4 執行緒、jsonl 快取可續跑、retry 退避、point-in-polygon 歸里＋NLSC 回應里名交叉驗證）。煙霧測試 166 筆取得座標、165 筆歸里，里名一致率 99.8%（修掉全形數字與正則貪婪兩個 bug）。使用者決定：全量 geocode（約 9–12 小時）由使用者事後自行補跑，指令見 README.md。驗收（成功率 ≥80%）待全量完成。
- 2026-07-18 步驟 9：pipeline/aggregate.py 完成（里×全部/中古/預售、近8季→12季擴窗、<10 筆標 insufficient）。以部分資料（快取 ~2,600 地址）機械性驗證通過；insufficient 比例與大安區抽查驗收待資料補齊。
- 2026-07-18 步驟 10–11：web/index.html 完成（MapLibre + OSM 瓦片、三視圖切換、預售/中古/全部切換、總價滑桿灰化、3D extrusion、hover 顯示里名數值、insufficient 灰色）。注意：Claude 瀏覽器面板不支援 rAF 無法截圖驗證，肉眼驗收需在一般瀏覽器開 http://localhost:8642/web/，待資料補齊後執行。發現：面板中 map load 事件不觸發，已改為 style.load 即初始化（順帶提升瓦片源故障時的韌性）。
- 2026-07-18 步驟 12：update.py 完成（下載→解析→清洗→村里界→geocode（吃快取）→聚合）。冪等驗收待 geocode 完成後執行（目前背景有 geocode 程序在寫快取，不可並行重跑）。
- 2026-07-19 步驟 13 前置：使用者要求所有程式趁現在寫完。新增 pipeline/priority_geocode.py（關鍵行政區優先抽樣 geocode）與 pipeline/qa.py（步驟 8/9/13 驗收全自動化，產出 reports/final_qa.md；--partial 模式供資料未全量時驗證程式路徑）。優先跑 11 個關鍵行政區共 5,616 地址後，QA 十項常識走查**全部 PASS**（大安 109.8 萬/坪 vs 全體 57.0；信義總價 2,254 萬；萬華<大安；林口/中壢預售>中古；烏來/湖田里/復興區全灰），僅步驟 8 成功率與步驟 9 insufficient 比例因資料未全量標 PARTIAL。收尾流程見 README.md，之後不需 AI 介入。
